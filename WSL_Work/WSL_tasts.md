This is what I am trying to do on the WSL side.

```console
$ sudo apt update && sudo apt upgrade -y
```

```console
$ sudo apt install ansible-core
```

```console
/evertj$ ansible-galaxy collection install community.general
```

